// ignore_for_file: prefer_const_constructors, use_key_in_widget_constructors, avoid_renaming_method_parameters

import 'package:flutter/material.dart';

class LoginPage extends StatelessWidget {
  @override
  Widget build(BuildContext build) {
    return Material(
      child: Center(
        child:  Text('Login page')
      ),
    );
  }
}
